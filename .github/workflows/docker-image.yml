name: Docker Image CI

on:
  push:
    branches: [ main, feature/Docker ]
  pull_request:
    branches: [ main ]

jobs:

  build:

    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v3
    - name: Run an echo
      run : echo "My Message"
    - name: Build the Docker image
      run: docker build --tag my-image-name:test .
